[application]
host = "0.0.0.0"
port = 8080
body_limit = "1MB"
allowed_mime_types = ["image/jpeg", "application/pdf"]
request_timeout_seconds = 2

[my-custom-section]
custom_key = "value"
env_user = "${USER:default_value}"